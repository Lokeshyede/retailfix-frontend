# Optional: URL of the RetailFix API backend.
# If left empty, it dynamically detects the hostname from the browser URL (highly recommended for local network and mobile testing).
VITE_API_URL=
VITE_APP_NAME=RetailFix
